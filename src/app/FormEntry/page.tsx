import { Box } from "@mui/material";
import ResponsiveGrid from "@/components/BasicGrid";

export default function FormPage() {
  return (
    <Box sx={{ height: '100vh' }}>
      <ResponsiveGrid />
    </Box>
  );
}
